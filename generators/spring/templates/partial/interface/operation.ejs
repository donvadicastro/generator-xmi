<%- operation.isReturnArray ? 'List<' : '' %><%= operation.returnParameter.typeRef ? `${operation.returnParameter.typeRef.namePascal}` : operation.returnParameter.type %><%- operation.isReturnArray ? '>' : '' %>
<%= operation.name %>(<% operation.inputParameters.forEach((x, i, arr) => { %>
    <%= x.typeRef ? `${x.typeRef.namePascal}${x.isArray ? '[]' : ''}` : x.type %> <%= x.name %><% if(arr.length - 1 > i) { %>,<% } %>
<% }) %>)
