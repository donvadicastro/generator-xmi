<%= entity.generalizationLinksTo ? `${entity.generalizationLinksTo.name}Base` : (entity.elementRef ? entity.elementRef.name : 'AbstractComponent') %>
    implements <% if ((entity.provided || []).length) { %><%= entity.provided.map(x => x.typeRef.namePascal).join(', ') %><%} else {%><%= entity.namePascal %><% } %>
