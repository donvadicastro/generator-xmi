package com.generator.design.<%= entity.getPathFromRoot(utils.self, '.') %>.controllers;

import com.generator.common.controllers.AbstractResourceController;
import com.generator.design.<%= entity.getPathFromRoot(utils.self, '.') %>.components.<%= entity.namePascal %>;
import com.generator.design.<%= entity.getPathFromRoot(utils.self, '.') %>.repositories.<%= entity.namePascal %>Repository;
import io.swagger.annotations.Api;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController("<%= entity.elementId %>_api")
@RequestMapping("<%= entity.getPathFromRoot(utils.kebabCase) %>/<%= utils.kebabCase(entity.name) %>")
@Api(tags = "Entities: <%= entity.namePascal %> (/<%= entity.getPathFromRoot(utils.kebabCase) %>)")
public class <%= entity.namePascal %>Controller extends AbstractResourceController<<%= entity.namePascal %>> {
    public <%= entity.namePascal %>Controller(@Qualifier("<%= entity.elementId %>") <%= entity.namePascal %>Repository repository) { super(repository); }
}
