constructor(
<% (entity.lifelines || []).forEach(function (lifeline, index) { %>
    /**
    * <%= lifeline.elementRef.name %> component
    */
    private cmp<%= lifeline.elementRef.name %>:  <%= lifeline.elementRef.namePascal %>Contract,
<% }) %>
) {}
