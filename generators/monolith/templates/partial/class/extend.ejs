<%= entity.generalizationLinksTo ? `extends ${entity.generalizationLinksTo.namePascal}` : (entity.elementRef && `extends ${entity.elementRef.name}`) %>
    implements <%= entity.namePascal %>Contract<% if ((entity.provided || []).length) { %>, <%= (entity.provided || []).map(x => x.namePascal + 'Contract').join(', ') %><%}%>
