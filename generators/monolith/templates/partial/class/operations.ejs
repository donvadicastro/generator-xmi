<% (entity.operations || []).forEach(function (operation) { %>
    /**
    * <%= operation.name %> description.
    */
    <%= operation.name %>(state: any): Promise<<%= operation.returnParameter.typeRef ? `${operation.returnParameter.typeRef.name}Contract` : operation.returnParameter.type %>> {
        return new Promise((resolve, reject) => {
            this.notifyComplete('<%= entity.name %>::<%= operation.name %>', state.start);
            resolve(state);
        });
    }
<% }) %>
