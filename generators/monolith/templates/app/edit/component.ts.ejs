import { Component } from '@angular/core';
import {FormBuilder, FormGroup, Validators} from '@angular/forms';
import {ActivatedRoute, Router} from '@angular/router';
import { HttpClient } from '@angular/common/http';

@Component({
  selector: 'app-<%= entity.name %>-edit',
  templateUrl: './component.html',
  styleUrls: ['./component.sass']
})
export class <%= entity.name %>EditComponent {
  private apiRootUrl = environment.api.url + '/<%= entity.name %>';

  public data: any = {};
  public form: FormGroup;

  constructor(private fb: FormBuilder,
              private router: Router,
              private activatedRoute: ActivatedRoute,
              private http: HttpClient) {
  }

  ngOnInit() {
    this.loadData();
    this.form = this.fb.group({
    <% (entity.attributes || []).filter(x => x.name).forEach((attr) => { %>
      <%= attr.name %>: ['', Validators.required],
    <% }) %>
    });
  }

  protected save() {
    this.http[this.data.id ? 'put' : 'post'](this.data.id ? `${this.apiRootUrl}/${this.data.id}` : this.apiRootUrl, this.form.getRawValue())
      .subscribe(() => this.router.navigate(['..'], { relativeTo: this.activatedRoute }));
  }

  protected delete() {
const url = `${this.apiRootUrl}/${this.data.id}`;

    if(window.confirm('Confirm delete')) {
      this.http.delete(url)
          .subscribe(() => this.router.navigate(['..'], {relativeTo: this.activatedRoute}));
    }
  }

  private loadData() {
    this.activatedRoute.params.subscribe(params => {
      if (params['id']) {
        this.http.get(`${this.apiRootUrl}/<%= entity.name %>/${params['id']}`)
          .subscribe(res => {
            this.data = <any>res;
            this.form.patchValue(this.data)
          });
      }
    });
  }
}
