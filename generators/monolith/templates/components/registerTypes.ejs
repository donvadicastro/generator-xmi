const DITypes = {
    ICommonDbManagerContract: Symbol.for('ICommonDbManager'),

<% components.filter((x, i, list) => list.indexOf(x) === i).forEach(x => { %>
    <%= x.entity.elementId %>: Symbol.for('<%= x.entity.elementId %>'),
    <% x.entity.provided.forEach(y => { %><%= y.elementId %>: Symbol.for('<%= y.elementId %>'),<% })}) %>
};

export {DITypes};
