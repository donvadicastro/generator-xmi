<%
const types = {};

components.filter((x, i, list) => list.indexOf(x) === i).forEach(x => {
    types[x.entity.elementId] = x.entity;
    x.entity.provided.forEach(y => types[y.typeRef.elementId] = y.typeRef);
});

%>

const DITypes = {
    ICommonDbManagerContract: Symbol.for('ICommonDbManager'),

    <% Object.keys(types).forEach(x => { %>
        <%= x %>: Symbol.for('<%= x %>'),
    <% }) %>
}

export {DITypes};
