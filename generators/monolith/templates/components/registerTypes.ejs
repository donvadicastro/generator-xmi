const DITypes = {
    ICommonDbManagerContract: Symbol.for('ICommonDbManager'),

<% components.forEach(x => { %>
    <%= x.entity.namePascal %>Contract: Symbol.for('<%= x.entity.namePascal %>Contract'),
    <% x.entity.provided.forEach(y => { %><%= y.namePascal %>Contract: Symbol.for('<%= y.namePascal %>'),<% })}) %>
};

export {DITypes};
