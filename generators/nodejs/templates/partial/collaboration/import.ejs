const chalk = require("chalk");
const storage = require('node-persist');

import {FlowState} from '<%= entity.relativeRoot %>/types/flowState';

<%- include('../common/importReferences', {entity: entity, deepLevel: ''}) -%>
