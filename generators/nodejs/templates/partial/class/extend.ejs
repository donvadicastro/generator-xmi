<%= entity.generalizationLinksTo ? `extends ${entity.generalizationLinksTo.elementId}` : (entity.elementRef && `extends ${entity.elementRef.name}`) %>
    <% if ((entity.provided || []).length) { %>implements <%= (entity.provided || []).map(x => x.elementId).join(', ') %><%}%>
