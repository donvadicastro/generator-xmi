import express from "express";
import Controller from "./controller";
import ActorSystemFactory from "../../<%= entity.getRelativeRoot() %>/api/server/actors";

const controller = new Controller(ActorSystemFactory.get());

export default express.Router()
    .post('/', controller.run.bind(controller));
