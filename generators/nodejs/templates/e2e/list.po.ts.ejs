import {ListPageObject} from "../../<%= entity.relativeRoot %>/base/administration/list.po";

export class <%= entity.namePascal %>ListPage extends ListPageObject {
    constructor() {
        super();
        this.baseUrl = 'administration/<%= entity.getPathFromRoot() %>/<%= entity.name %>';
    }
}
