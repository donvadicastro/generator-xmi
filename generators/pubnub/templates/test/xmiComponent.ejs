import {<%= name %>} from '../process/<%= name %>';

<% (lifelines || []).forEach(function (lifeline, index) { %>
import {<%= lifeline.elementRef.name %>} from '../components/<%= lifeline.elementRef.name %>'; <% }) %>

const process = new <%= name %>(<% (lifelines || []).forEach(function (lifeline, index) { %>
    new <%= lifeline.name %>(<%= index > 0 ? array[index - 1].lifeline.name : '' %>),
<% }) %>);

process.run(0).then(ret => console.log(ret));