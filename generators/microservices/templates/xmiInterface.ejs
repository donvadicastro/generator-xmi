import {IMessageBase} from '../<%= entity.relativeRoot %>/contracts/messages/baseMessage';

export interface <%= entity.namePascal %>Message extends IMessageBase {
    <% (entity.attributes || []).forEach(function (attr) { %>
    /**
    * <%= attr.name %> property
    */
    <%= attr.name %>: <%= attr.typeRef ? attr.typeRef.name + 'Contract' : attr.type %>;
    <% }) %>
}
